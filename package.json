{
  "scripts": {
    "fetch": "git submodule update --remote",
    "generate": "ts-node ./scripts/generate.ts",
    "bump": "ts-node ./scripts/bump.ts",
    "publish": "npx -p elm@0.19.0-no-deps elm publish"
  },
  "devDependencies": {
    "elm-codegen": "^0.5.2",
    "elm-tooling": "^1.15.0",
    "ts-node": "^10.9.2"
  }
}